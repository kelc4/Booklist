package ui;

import model.Book;

import javax.swing.*;
import java.awt.*;

//Creates the gui screen for the booklist app
public class BookListAppGUI {

    public void runGUI() {
        /*
        JFrame frame = new JFrame();  //Creates jframe

        //Top of frame
        frame.setTitle("BookList Application");
        ImageIcon imageIcon = new ImageIcon("b.jpg");
        frame.setIconImage(imageIcon.getImage());   //Sets icon on top left of frame
        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //makes program stop running after closing frame

        //About the frame
        frame.getContentPane().setBackground(new Color(55, 0, 85));
        frame.setResizable(false); //prevents frame from being resized
        frame.setSize(650, 420); //x, y dimensions of frame
        frame.setLocationRelativeTo(null);  //Centers frame
        frame.setVisible(true); //makes frame visible

        //Inside the frame
        JScrollPane scrPane = new JScrollPane(frame);
        frame.getContentPane().add(scrPane);
        */

    }

    public void addBook(Book book) {

    }

}
